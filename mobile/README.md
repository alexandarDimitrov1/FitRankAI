# FitRank AI Mobile

React Native / Expo app scaffold for the FitRank AI mobile experience.

## Setup

```bash
npm install
npm run start
```

Set `EXPO_PUBLIC_API_URL` to the Flask backend URL when testing on a device.
